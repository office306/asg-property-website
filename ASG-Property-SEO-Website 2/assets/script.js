document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.enquiryForm').forEach(function(form){const status=form.parentElement.querySelector('.formStatus');const frame=form.parentElement.querySelector('.submissionFrame');const button=form.querySelector('button[type=submit]');let submitted=false;form.addEventListener('submit',function(){submitted=true;button.disabled=true;button.textContent='Sending…';if(status){status.style.display='block';status.textContent='Sending your enquiry…';}});if(frame)frame.addEventListener('load',function(){if(!submitted)return;if(status)status.textContent='Thank you. Your enquiry has been received.';button.disabled=false;button.textContent='Send enquiry';form.reset();submitted=false;});});});